<%= ur_fields.fields_for :answers do |answer_fields| %>
  <%= answer_fields.collection_check_boxes :choice_id, question.choices, :id, :option do |cb| %>
      <p><%= cb.check_box %> <%= cb.label %></p>
    <% end %>
<% end %>